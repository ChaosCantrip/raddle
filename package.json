{
  "name": "raddle",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^9.39.4"
  },
  "scripts": {
    "build": "npm run build -w @raddle/types && npm run build -w @raddle/common && npm run build -w @raddle/api && npm run build -w @raddle/website"
  }
}
